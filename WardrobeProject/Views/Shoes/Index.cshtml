@model IEnumerable<WardrobeProject.Models.Shoe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.shoeItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.photo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.shoeItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.photo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.shoeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.shoeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.shoeID })
        </td>
    </tr>
}

</table>
