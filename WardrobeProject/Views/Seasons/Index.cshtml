@model IEnumerable<WardrobeProject.Models.Season>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SeasonsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SeasonsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SeasonsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SeasonsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SeasonsID })
        </td>
    </tr>
}

</table>
