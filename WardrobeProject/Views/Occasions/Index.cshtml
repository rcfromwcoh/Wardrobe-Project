@model IEnumerable<WardrobeProject.Models.Occasion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OccasionsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OccasionsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OccasionsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OccasionsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OccasionsID })
        </td>
    </tr>
}

</table>
